

// file should exetenction FileName.conf 
// location Where to find files for this path 

    
//     allow methods 
//     listing off on 
// server{
//         Purpose: defines one server instance with IP, port, error pages, and location blocks.

//         Mandatory attributes:

//         listen → IP and port

//         client_max_body_size → max request size

//         At least one location block

//         Optional attributes:

//         server_name

//         error_page (other than default)

//         Any custom global directives

//         locatoin {
            
//             Purpose: define rules for a specific URL path.

//             Mandatory attributes inside a location:

//             root

//             index

//             allow_methods

//             autoindex

//             Optional attributes:

//             cgi_extension + cgi_path

//             upload_store

//             return <code> <url>; for redirection
//             diractives{
//                 Examples:
//                 root /var/www/html;

//                 index index.html;

//                 allow_methods GET POST;

//                 autoindex on;

//                 cgi_extension .php;

//                 cgi_path /usr/bin/php-cgi;

//                 upload_store /var/www/uploads;

//                 error_page 404 /errors/404.html;

//                 client_max_body_size 2000000;
//             }

//         }
    
//     }

//     RULES{
     
    
//     Every directive ends with ;

//     Every block { ... } is properly closed

//     Each server must have at least one location

//     Each location must define mandatory attributes

//     Only recognized directives are allowed

//     Nested blocks must follow proper order (server → location)
//}

    

toknizer is good to parsing like {} and path location 
port var should be like IP:PORT 
readme resource 

there default  tome   to disconnect

bad request is line by line check first thing and second else bad request will return 


First read request	Register EPOLLIN
Want to send response	send() as much as possible
Not all sent	Enable EPOLLOUT to continue sending later
All sent	Disable EPOLLOUT

/////////////////////////////////////////////////////////////////////------------
parsing of conif file i fed up with it so i give that my power and i will 
run nginx in varbose mod to see his realy behave 
\\\\\\\\\\\\\\\\\\\\\\\\\\\\
ideas -> 


steps to IO handling 
so -> ok 
       EVENT ERRORs 
              check this EPOLLHUP & EPOLLER close connectoin after got this event so try 
       EPOLLIN 
              reading first header limit \n\n\n so or body  max size headers complety 
              [if body and header done  go to build request ] if ziro remove this client if -1 wait data if still needed 
              you can  know how much data in socket without reading 

              go to body reading 
                     small body reading once use IOCTL know 
                     big body file posting -> i should read file and put in it if delete repon that and put data 
                     if not file i should put in such file u randome it name 
                     the body limit content lenght no content lenght is bad request i thinks 
                     and by config max body 
                     read 
       EPOLLOUT
              

